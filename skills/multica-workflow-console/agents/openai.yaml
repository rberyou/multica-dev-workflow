interface:
  display_name: "Multica Workflow Console"
  short_description: "Read-only workflow status and gate inspection"
  default_prompt: "Use $multica-workflow-console to validate the workflow checkout and inspect current Multica drift without mutating runtime state."
